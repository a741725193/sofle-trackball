include:
  - board: eyelash_nano
    shield: sofle_trackball_left nice_view
  - board: eyelash_nano
    shield: sofle_trackball_right nice_view
  - board: xiao_ble//zmk
    # TEMP: swapped board from eyelash_nano to xiao_ble to test on the
    # hardware Prospector was actually developed/verified against.
    shield: sofle_trackball_dongle prospector_adapter
    snippet: studio-rpc-usb-uart zmk-usb-logging
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
  - board: eyelash_nano
    shield: settings_reset
